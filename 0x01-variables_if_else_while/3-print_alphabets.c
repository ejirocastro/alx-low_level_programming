#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (success) valiod
 */
int main(void)
{
	char n;

	char m;

	n = 'a';
	m = 'A';
	while
		(n <= 'z') {
			putchar(n);
			n++;
		}
	while
		(m <= 'z') {
			putchar(m);
			m++;
		}
	putchar('\n');
	return (0);
}
