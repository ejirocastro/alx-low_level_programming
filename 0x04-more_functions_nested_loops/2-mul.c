#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: one of the integer to be multiplied
 * @b: the second interger multiplied
 * Return: result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
