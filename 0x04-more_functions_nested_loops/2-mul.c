#include "main.h"

/**
 * mul - This is a function that multiplies two numbers
 * @a: Is the first integer
 * @b: is the second interger
 * Result: is the variable that stores the result
 * Return: Result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
