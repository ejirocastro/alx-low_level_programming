#include <string.h>

/**
 * _strlen - a function that returns the length of a string.
 * @s: length of a string been returned
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
