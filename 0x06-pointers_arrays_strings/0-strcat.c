#include <string.h>
#include "main.h"

/**
 * *_strcat - a function that concatenates two strings.
 * @dest: where src will be stored
 * @src: a pointer to the source string
 * Return: _strcat
 */

char *_strcat(char *dest, char *src)
{
	return (_strcat(dest, src));
}
